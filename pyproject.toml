[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "my-trader"
version = "0.1.0"
description = "NautilusTrader strategy package for OKX. The node is the runner."
readme = "README.md"
requires-python = ">=3.12,<3.15"
dependencies = [
  "nautilus_trader",
]

[project.optional-dependencies]
dev = [
  "pytest>=8",
]
etf = ["yfinance"]

[project.scripts]
backtest-okx = "sngw_trader.runners.backtest_okx:main"
live-okx = "sngw_trader.runners.live_okx:main"
wf-okx = "sngw_trader.research.walk_forward:main"
catalog-download = "sngw_trader.data.catalog_writer:main"
regime-download = "sngw_trader.data.regime_writer:main"
regime-stage0 = "sngw_trader.research.regime_stage0:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
